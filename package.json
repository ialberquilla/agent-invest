{
  "name": "agent-invest",
  "private": true,
  "packageManager": "pnpm@10.19.0",
  "scripts": {
    "preinstall": "node scripts/enforce-pnpm.cjs",
    "dev": "pnpm --filter @agent-invest/agent dev",
    "build": "pnpm -r --if-present build",
    "db:migrate": "pnpm --filter @agent-invest/agent db:migrate",
    "ingest:gmx": "pnpm --filter @agent-invest/agent ingest:gmx",
    "ingest:coingecko-market-caps": "pnpm --filter @agent-invest/agent ingest:coingecko-market-caps",
    "contracts:build": "pnpm --filter @agent-invest/contracts build",
    "contracts:test": "pnpm --filter @agent-invest/contracts test",
    "contracts:fmt": "pnpm --filter @agent-invest/contracts fmt",
    "py:lock": "sh -c 'cd agent/scripts && uv lock \"$@\"' --",
    "py:sync": "sh -c 'cd agent/scripts && uv sync \"$@\"' --",
    "py:sync:frozen": "sh -c 'cd agent/scripts && uv sync --frozen \"$@\"' --",
    "test": "pnpm --filter @agent-invest/agent test",
    "lint": "eslint .",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "eslint": "^9.26.0",
    "eslint-config-prettier": "^10.1.5",
    "globals": "^16.0.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.32.0"
  }
}
